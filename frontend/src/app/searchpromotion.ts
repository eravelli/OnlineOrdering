import { stringify } from "@angular/compiler/src/util";
import { NoSsr } from "@material-ui/core";

export class searchpromotion{
    
    constructor(promoId: number, 
        promoName: string,
        promoDesc: string,
        promoLogo: string,
        zoneId: number,
        zoneName: string,
        breakStartDate: Date,
        breakEndDate: Date
       ){
        
    }
}